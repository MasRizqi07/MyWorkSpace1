const name = 'Pacol';
const age = 18;
const born = 2007;
const job = 'Mahasiswa';

const person = {
    name: name,
    age: age,
    born: born,
    job: job
}
const text = person.name + ' Ini adalah ' + person.job + ' yang berumur ' + person.age + ' tahun dan Lahir pada tahun ' + person.born + '.';
console.log(text);
